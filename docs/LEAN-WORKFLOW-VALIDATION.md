# 质量优先接力与轻量渲染验证记录

日期：2026-09-07。v1.1.0 源码与安装包采用用户选定的 Y 角色接力。
以下区分 Y 方案验证和较早 `a8f4c14` 单创作者技术实验，
不将旧实验成绩当作当前完整流程的成绩。

## Y：同输入对照与用户选择

同一份虚构知识整理工具 SRT/design、同一套本地字体和 GSAP，四镜各8秒。
X 是 `a8f4c14` 单创作者；Y 是独立导演、两个各负责连续两镜的新制作上下文，
加少量具体动作参照与独立审美。所有本轮选定的子 Agent/CLI 模型均为 GPT 5.6 Sol。

两片实际输出都是 H.264、1920×1080、30fps、960帧、32.000秒、无音轨，
完成真实探测与完整解码。独立 Sol 在隐藏来源后查看八张六帧联系表，并对双方
7.4–8.9秒及23.4–24.9秒两个接缝作连续采样；它更偏向 Y 的层次、物体继承与
动作因果。它没有观看完整视频，因此不把关键帧评审冒充完整连续观影。

用户随后明确选择“按照 Y＝新方案执行，最终效果好大于一切”。这是采用该
视觉与工作方式的依据，不代表每个细节都无缺陷：Y 第一镜末尾过早出现已归档
的解决感、第二镜细线与小字偏弱、末镜还可进一步让工具退场。

| 模型任务记录（含任务内部故障排查） | X 单创作者 | Y 导演＋两位制作 |
| --- | ---: | ---: |
| 第一任务启动至最后任务返回，计入并行 | 325.136秒 | 611.281秒 |
| 输入 Token（包含缓存） | 307,572 | 1,672,328 |
| 其中缓存输入 | 271,744 | 1,545,344 |
| 非缓存输入 | 35,828 | 126,984 |
| 输出 Token（已含推理） | 15,911 | 37,888 |

共享盲评另用201.203秒，输入263,145（缓存227,840）、输出8,760。最初两次
执行器启动失败另有输入145,360（缓存103,424）、输出1,654。辅助审计和父级
本会话的完整 Token 未测；不能将这些表格当整轮总账，也不能直接换算费用。

本轮存在执行器路径故障、宿主仍注入大量 Skill、导演无效搜索，以及嵌套环境
不能启动渲染服务等干扰。两组由父级在可用环境中执行真正渲染；X 的 S04 还
移除了固定运行时不支持的字距动画。因此这不是两个完整自动闭环的干净速度
基准，**不证明节省时间或 Token，也不证明接力架构固定慢某个比例**。

在 Y 的独立副本中只修改 S02 强调色：8.569秒完成更新，仅 S02 的媒体哈希
改变，其余三镜逐字节不变，完整预览更新并通过实际解码。该机械改色未改变
用户审看的 Y 样片。这个结果支持保留局部缓存，不等于端到端创作提速。

## 当前实现如何承接 Y

- 入口恢复独立导演、章节制作、独立审美与原作者返修，父级负责机械脚本。
- `prepare-creative-task.mjs` 提前检查缺资料和镜头范围，写带真实绝对路径的
  短任务文件；制作角色只拿本章及相邻接缝，审美角色只拿需求、方向与实际媒体。
  工具不启动模型，也不能替宿主清除所有全局注入内容。
- 三种紧凑动作参考配一份原创可执行示例，避免只加抽象审美禁令；不是固定皮肤。
- 固定版指导明确首帧状态、局部资产路径、字体、有限时间线和可支持的动画属性。
- 效果优先于减少角色数；复用已有媒体和源码，减少无效搜索和重复机械工作。

## 接力实现后的回归与前向检查

- 完整 `npm test`：**233/233 通过，无跳过**；14个项目 Skill 通过官方校验。
  新增5项交接行为测试覆盖输入清单、缺资料、连续镜头、共享源码冲突、实际绝对
  命令，以及同时存在 draft/final 时要求明确审看版本。原有发布包与安装回归通过。
- 一位全新 Sol 导演仅接收生成的任务文件，完成真实32秒原始输入的计划、共用
  方向和4张镜头卡，计划校验通过。执行记录未读取全局安装的旧 Skill；但宿主
  仍提示 Skill 目录过长，不能声称已经清除了全局注入或严格限制了全部上下文。
- 真实导演交付可以生成两个连续制作任务；实际 Y 成片可以生成四镜审美任务。
  审美输入没有制作源码和带成本指标的原始渲染索引，需求和实际媒体路径齐备。
  这是角色交接的前向检查，不是又一次完整成片生产基准。
- 原创8秒动效示例在固定 HyperFrames 0.7.104 下实际渲染与解码，1920×1080、
  30fps、240帧。查看联系表和完整尺寸落点帧后修掉了首帧纯空底问题。
- 用户样片、原始字幕、模型日志和验证工程均留在仓库外。首页只说明工作方式和验证范围，
  不展示测试样片；发行 Skill 包不携带测试数据、字体或 GSAP 副本。

## v1.1.0 安装包与首页修复

五语 README 恢复原展示结构、两份历史动图、流程图和作者联系方式；中文作者区域
与更新前版本逐字一致，原动图和微信二维码文件逐字节一致。流程图保留原布局，
文字同步新接力。Y 测试样片没有加入首页或安装包。

标准 Skill 包与完整环境包的 500 个项目 Skill 文件逐字节一致。完整包实际解压后，
使用隔离的用户注册目录和应用目录执行安装：44 条链接正确，版本为 1.1.0，
doctor 为 ready，安装后的父 Skill 确实包含新导演、制作、审美与交接入口。
这次复用机器已有 Node、FFmpeg 和浏览器，不冒充全新 macOS 机器的冷安装测试；
没有替换用户正在使用的全局 Skill 安装。

## 较早 a8f4c14 技术实验（历史，单创作者默认已替换）

本次验证说明新流程可以实际出片和局部重做；不证明所有题材、长片或
同输入成片的审美质量已经优于旧版。

## 较早实验的改动与验证范围

- 新项目使用一个创作负责人、一份 `broll-plan.json` 和每镜独立 HTML。
  长片可按连续章节分工；普通任务不再固定经过五个制作角色和两轮样片门禁。
- 原始 SRT/design 保留。程序检查时间覆盖、实际输出、镜头依赖与复用。
- 真实主体优先采用可识别的素材。SVG 保留图标、真实数据图、遮罩和关系表达用途。
- 原有 Recipe/runtime-plan v1–v4、Remotion/hybrid 和明确要求的版本比较走兼容入口。
  本次不修改旧项目状态，也不将旧项目自动迁移到新计划。

## 真实媒体验证

macOS，本地固定 HyperFrames 0.7.104、FFmpeg。输入为 24 秒、4 条各 6 秒的
合成 SRT，依次讲相机实物、前后比较、清楚的信息、简洁结论。预先准备一张
合法相机照片、本地字体和 GSAP。独立创作 Agent 根据原始输入与新 Skill 制作
四镜头，没有被提供目标 HTML。数字明确标注为演示数据。

首次源码写入到草稿复查约 5 分 08 秒；该区间包含创作、报错修正和两次整段
草稿输出，不包含预先寻找素材、环境准备或本次工程改造。

最终渲染器版本的实际调用结果：

| 操作 | 重渲镜头 | 复用镜头 | 耗时 |
| --- | ---: | ---: | ---: |
| 1080p/30fps 正式输出，含检查图与完整预览 | 4 | 0 | 25.348 秒 |
| 不改任何内容，再运行一次 | 0 | 4 | 0.174 秒 |
| 只改变 S02 的强调色，再输出 | 1 | 3 | 7.811 秒 |
| 960×540/15fps 草稿，含检查图与完整预览 | 4 | 0 | 17.322 秒 |

S02 修改在独立测试副本内完成：只有 S02 MP4 的 SHA-256 改变，S01/S03/S04
保持完全一致，完整预览重新组装。草稿和正式输出各 24.000 秒，H.264、无音轨，
经过完整解码检查。已检查四镜头六帧图和整段预览抽帧，并在浏览器确认高清
预览正常播放到末段；这不等于用户已经认可其审美。

前向测试发现并修正的实际问题：

- 固定 HyperFrames 版本需要显式初始化 `window.__timelines`、HTML 内的
  `@font-face`，以及相对项目根目录的素材路径；新版通用示例不能直接照搬。
- S02/S03 首帧过空，已使照片/核心信息在首帧可见。
- 渲染失败不再因 `--quiet` 丢失关键报错，返回具体失败信息供原作者修正。
- 每镜输出按全局帧边界分配，避免毫秒时间逐镜取整积累误差。
  自动化回归使用 20 段 × 101ms：30fps 下总计 61 帧，而非独立向上取整的 80 帧。

## Token 实验：只测计划首轮，不能作为等质量成片比较

同一份 24 秒 SRT/design，Codex CLI 0.153.4、`gpt-5.4-mini`、medium reasoning。
两个隔离工作目录分别读取公开 v1.0.1 Director 契约与新的轻量计划契约。
禁止出片、搜素材和委派；旧版按要求写 Director 交接，新版只写一个计划。
两个首轮任务并行执行。日志放在工作目录之外，避免模型误读自己的日志。

| 宿主实际报告 | 旧版 Director 首轮 | 轻量计划首轮 |
| --- | ---: | ---: |
| 用时 | 219.954 秒 | 60.049 秒 |
| 输入 Token（包含缓存） | 172,711 | 98,730 |
| 其中缓存输入 | 142,080 | 78,592 |
| 非缓存输入 | 30,631 | 20,138 |
| 输出 Token | 14,371 | 3,043 |
| 其中推理 Token（已包含于输出） | 7,990 | 1,232 |

轻量计划通过原始 SRT 与计划校验。旧版首轮未通过：`truth.sourceCues` 写成了
整数，旧校验要求字符串。另一次修复调用在 300 秒超时前仍未形成可验收交接，
且修复过程越过了限定读取范围，故排除其结果；没有完整宿主用量，不能把它
记成零 Token，也不能纳入有效比较。更早一轮存在日志污染，整体排除。

这组首轮数字支持“减少交接文件和重复推理值得继续验证”，不能支持“等质量
成片节省某个百分比”。缓存 Token 不等同于非缓存费用；未测金额、整片 Token、
重复实验方差或 Claude Code 同输入结果。

## 当前限制与复现入口

- 这是小型技术样片，题材单一、素材已备；尚无新版完整长片对照；Y 的32秒盲评范围见上文。
- 共享素材、原始设计或共享代码改变时，当前实现保守地使所有镜头失效。
  只改独立镜头源码时局部重渲；不是精确到每张图片使用者的完整依赖分析。
- 1080p 源码的草稿仍先按原始尺寸捕获，再降采样；固定运行时没有任意
  sub-1080p 捕获档位。自定义正式尺寸必须与 HTML 原生尺寸相符。
- 新计划接受按时间排列、不重叠的普通 SRT；重叠字幕需先明确应如何解释，
  程序不会悄悄修改原始字幕。
- 新路径先覆盖 HyperFrames。已有后端和审批逻辑的回归测试保留；未做新版本发布、
  跨平台重新安装或全局 Skill 切换。

制作与局部修改入口见 [轻量制作命令](../erduo-broll-loop-engineering/references/lean-production.md)。
新增计划/渲染回归覆盖真实 CLI 入口、短片、时间覆盖、缓存失效、部分预览、
失败续跑、解码失败和累计帧边界。运行 `npm test` 检查完整新旧契约。
`a8f4c14` 基线完整测试 **228/228 通过、无跳过**；其中计划/渲染测试24项。
14 个项目 Skill 通过官方 `quick_validate.py`，`git diff --check` 通过。

测试资产来源：[Jason Zhang 的 CC0 相机照片](https://commons.wikimedia.org/wiki/File:Canon_EOS_Rebel_T5_with_18-55_mm_lens.jpg)、
[Noto 字体许可](https://github.com/notofonts/noto-cjk/blob/main/Sans/LICENSE)。
媒体、原始输入、宿主日志和测试工程留在本地验证目录，不进入发行包。
